#!/usr/bin/env python
# -*- coding: utf-8 -*-
# Importaciones
from datetime import date, datetime, timedelta
from openerp.osv import osv, fields
from openerp import models, fields, api
# Configuramos

class GolBasicEntity(osv.Model):
    _name = 'gol.basic.entity'
    _description = u'descripcion modulo'
    #Decalaracion de campos
    code=fields.Char('Código',size=50,required=True,help='Ingrese el Codigo')
    name=fields.Char('Nombre',size=50,required=True,help='Ingrese el Nombre')
GolBasicEntity()
